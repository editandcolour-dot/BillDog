# Session Log — 2026-04-18

## 15:33 — PayFast Signature Fix: URL Encoding
- **Root cause identified by user:** PayFast hashes URL-encoded values, not raw values. Our hash string had `https://www.billdog.co.za/...` instead of `https%3A%2F%2Fwww.billdog.co.za%2F...`.
- Added `payfastUrlEncode()` function (PHP `urlencode` parity: `encodeURIComponent` + `%20→+`).
- Applied encoding to all parameter values in `generateSignature()`.
- Removed `email_confirmation: '0'` — not in PayFast's canonical example.

## 15:36 — Passphrase Encoding Correction
- User clarified: passphrase is appended RAW (not URL-encoded) in PayFast's example.
- Changed `payfastUrlEncode(passphrase)` → `passphrase.trim()`.

## 15:37 — Field Order Fix (Alphabetical → Canonical)
- **Critical discovery:** PayFast's example hash string uses a specific non-alphabetical order, not `ksort()`.
- Canonical order: `merchant_id → merchant_key → return_url → cancel_url → notify_url → name_first → email_address → m_payment_id → amount → item_name → subscription_type`.
- Replaced `Object.keys().sort()` with explicit `PAYFAST_FIELD_ORDER` array.
- Changed `generateSignature()` interface from `Record<string, string>` → `[string, string][]`.
- Updated `charge.ts` to pass ordered pairs instead of Record.

## 15:41 — Dry-Run Verification
- Created `test_payfast_dryrun.js` — zero network calls, prints exact hash string + MD5.
- **Result:** MD5 = `ebaaff7be19f69e092ea149e14be8846`
- Hash string matches PayFast's canonical format (URL-encoded values, raw passphrase, canonical field order).
- Awaiting user external MD5 verification.

## 15:46 — Memory Update
- Rewrote `payfast-diagnostic-memory.md` with corrected fault log.
- Updated `STATE.md` and session log.

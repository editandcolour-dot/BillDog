# Session: 2026-05-14

## [04:22] Session Start
- Status: New session
- Context loaded: PROJECT_MEMORY, TECH_STACK, FAULT_LOG, STATE, 2026-05-13 session
- ✅ Security preflight passed — 04:22

## Today's Plan
0. **SHIP BLOCKER**: Water/sewerage tier arithmetic validator producing false positives
1. Schema drift audit — five phantom column references found last night
2. Server-side profile creation trigger — fixes consent timestamp race (every signup)
3. UI bugs: Connect Municipality card showing after connection, timeline counter 0/36
4. Read the letter, audit 3 errors against analysis page, judge summary paragraph
5. Send the letter to CoCT (Monday morning ideally)

## [04:25] Schema Drift Audit — Complete
- Read all 33 migrations (001 through 20260401)
- Grepped every table/column reference across entire codebase
- **8 confirmed drift issues identified**:
  1. `autofetch_credentials` → wrong table name (should be `municipal_credentials`)
  2. `cases.property_address` → phantom column, never migrated
  3. `profiles.address` → phantom column, never migrated
  4. Dual escalation columns: `escalation_stage` (005) vs `escalation_step` (016)
  5. Dashboard credential query has wrong column names (`municipality_name`, `status`)
  6. Dual ID capture paths → user prompted twice
  7. BillTimeline counter "0 of 36" misleading message
  8. Consent timestamp race on signup
- Full audit written to `implementation_plan.md`
- **Waiting for user approval** on 2 open questions before coding schema fixes

## [04:35] SHIP BLOCKER — Tier Arithmetic Fix ✅
- **Root cause**: `checkLineArithmetic` in `universalChecks.ts` used `.match()` (first match only) instead of `.matchAll()` (all tiers)
- Multi-tier descriptions like `(1) 6.707 kl @ R 21.15 (2) 3.293 kl @ R 29.06` → only Tier 1 extracted → compared against combined billed amount → false positive
- **Fix**: Changed to `matchAll`, sum all tier expected amounts, compare sum vs billed. Tolerance raised from R0.10 to R1.00 for cross-tier rounding
- **7 regression tests** written and passing — covers 2-tier, 3-tier, single-tier, genuine overcharges
- **14 existing bill-validator tests** still passing
- **0 TypeScript errors**

## [04:57] Schema Drift PR — All 7 Priorities Executed ✅
- **Migration 035** — `case_events` table created (enables 41 silently-failing calls across 16 files)
- **Dashboard** — `autofetch_credentials` → `municipal_credentials`, correct column names, proper connected check
- **Escalation unification** — `escalation_stage` → `escalation_step` in 4 files (escalate-dispute, submit-id, case page, types)
- **Property address** — bill-text extraction replaces 3 phantom column reads (escalationEngine, escalate-dispute, case page)
- **ID capture** — `store_poppi_id` → `store_account_holder_id` with correct parameter names
- **cron_errors** — 2 phantom table inserts → console.error
- **Type definitions** — removed `escalation_stage` and `property_address` from Case type
- **Verification**: `tsc --noEmit` = 0 errors, `vitest run` = 314/314 passed, phantom grep = 0 hits
- **Ready for deploy** — user must run migration 035 SQL in Supabase after deploy

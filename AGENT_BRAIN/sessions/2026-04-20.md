# Session Log: 2026-04-20

## 20:25 — Tariff Resolver Architecture & Deep CoCT Parser Fixes
- **What was done:** 
  1. Converted `validateBill` and verifying components to native `async/await` structures and removed all hardcoded CoCT tariff proxies from the source tree.
  2. Implemented the dynamic `Supabase` Tariff Resolver caching and human review gap-queue system (`018_tariff_resolver.sql`).
  3. Rebuilt `seed-tariff-cache.ts` carefully ensuring only `verified: true` variables bounded by `20mm`/`240L` mathematical ground truth natively seeded inside Cache.
  4. Squashed 21 false positives by meticulously decoupling `RE_RATES_LINE` and `RE_REBATE_LINE` logic into segregated extraction steps avoiding VAT POISON errors.
  5. Decoupled and reconstructed `parseTierLines` merging split-tier water chunks effectively matching cross-line dependencies dynamically over universal VAT extractions.
  6. Successfully bound `wc.description` extracted service-start boundaries for correct June-July fiscal boundary `Fixed Basic` FY lookups instead of blindly falling back heavily on global invoice dates.

- **What changed:**
  - `lib/parsers/coct-bill-parser.ts` 
  - `lib/validators/bill-validator.ts`
  - `lib/tariff/verifiers/*`
  - `lib/tariff/tariff-resolver.ts` (New module)
  - `supabase/migrations/018_tariff_resolver.sql` (New db object)

- **Decisions & Issues:**
  - Hardcodes gracefully migrated into a network-aware interceptor safely falling back natively to `SKIP` rather than blindly erroring out.
  - Used explicit isolated parsing segments sequentially to eliminate overlapping string mismatches (`(?<!-)` fallback strategies replaced structurally by absolute match separation logic).
